# tthree 文档

这是 tthree 库的独立文档站点，使用 VitePress 构建。

## 📁 文档目录说明

项目中有两个 docs 目录，用途不同：

### 1. 根目录 `docs/`（统一文档站点）

- **位置**: `/docs/`
- **用途**: 整个 monorepo 项目的统一文档站点
- **内容**: 包含 nest、react、tthree 等所有包的文档概览
- **运行**: `pnpm docs:dev`（在项目根目录）
- **说明**: 作为项目整体文档入口，tthree 文档作为其中一个章节

### 2. `packages/tthree/docs/`（独立文档站点）

- **位置**: `/packages/tthree/docs/`
- **用途**: tthree 包的独立完整文档站点
- **内容**: 包含完整的指南、API 文档和使用示例
- **运行**: `pnpm docs:dev`（在 packages/tthree 目录）
- **说明**: 可以独立运行、构建和部署，适合单独发布

## 🚀 使用方法

### 开发文档

运行以下命令启动文档开发服务器：

```bash
cd packages/tthree
pnpm docs:dev
```

### 构建文档

运行以下命令构建文档：

```bash
pnpm docs:build
```

### 预览构建结果

运行以下命令预览构建后的文档：

```bash
pnpm docs:preview
```

## 📝 生成 API 文档

API 文档由 TypeDoc 自动生成，运行以下命令生成：

```bash
pnpm docs
```

生成的文档位于 `docs/api` 目录。

## 📚 文档结构

```
packages/tthree/docs/
├── .vitepress/          # VitePress 配置
│   └── config.ts
├── api/                 # TypeDoc 生成的 API 文档
│   ├── classes/
│   ├── interfaces/
│   └── index.md
├── guide/               # 指南文档
│   └── getting-started.md
├── index.md             # 首页
└── README.md            # 本文件
```
